{
  "name": "autodeck",
  "private": true,
  "version": "0.0.0",
  "description": "AutoDeck monorepo. Phase 0 (packages/domain, packages/design-tokens) and Phase 1 (backend bootstrap, auth/RBAC, audit infrastructure, Firestore rules) implemented — emulator-only, no real infrastructure.",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "test:rules": "firebase emulators:exec --project demo-autodeck --only firestore,auth \"jest --config tests/rules/jest.config.js\"",
    "test:backend:e2e": "firebase emulators:exec --project demo-autodeck --only firestore,auth \"pnpm --filter @autodeck/backend run test:e2e\"",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.9.0",
    "firebase-tools": "^13.6.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "uuid": "^9.0.1"
    }
  }
}
