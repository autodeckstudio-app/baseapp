{
  "name": "@autodeck/domain",
  "private": true,
  "version": "0.0.0",
  "description": "Pure, framework-agnostic business logic shared across all AutoDeck apps: pricing/advance calculation, refund-eligibility rules, and booking state machine are implemented (Phase 0). No Firebase, Express/Nest, or React dependency.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"domain lint: not yet implemented\"",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12"
  }
}
