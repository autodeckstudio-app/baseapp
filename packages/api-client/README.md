# @autodeck/api-client

Typed client for calling the AutoDeck backend API, shared by `customer-mobile`, `customer-web`, `studio-mobile`, and `admin-web`. Built on `@autodeck/domain`'s shared types/schemas so every app talks to the backend through one consistent, typed contract.

**Status**: repository scaffold only. No endpoints or client methods implemented yet.
