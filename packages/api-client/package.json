{
  "name": "@autodeck/api-client",
  "private": true,
  "version": "0.0.0",
  "description": "Generic, auth-aware HTTP client for calling the AutoDeck backend, shared by customer-mobile, studio-mobile, and admin-web. Phase 3A: base URL, token attachment, response parsing, and typed ApiError/NetworkError. No endpoint-specific methods yet — those arrive as each screen is built.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"api-client lint: not yet implemented\"",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@autodeck/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.0"
  }
}
